pipeline {
    agent any

    stages {

        stage('checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Inzemamskillops/zebronicx.git'
            }
        }

        stage('Deploy') {
            steps {
                sshagent(credentials: ['flipcart-server-2']) {
                    sh '''
                        scp -o StrictHostKeyChecking=no index.html ubuntu@3.110.222.233:/var/www/html/
                    '''
                }
            }
        }

    }
}
